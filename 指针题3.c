#include <stdio.h>

int main()
{
    int a[5][5];
    int (*p)[4]; //定义一个指向含有4个整数的一维数组的指针p
    p = a;
    //打印p[4][2]和a[4][2]的地址差，这里的地址差实际上为两个地址间相隔的元素数目
    printf("%p %d\n", &p[4][2] - &a[4][2], &p[4][2] - &a[4][2]);
    return 0;
}